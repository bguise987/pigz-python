[tox:tox]
requires =
    tox >= 4.23
env_list =
    3.14
    3.13
    3.12
    3.11
    3.10
    3.9
skipsdist = true

[testenv]
deps = pytest
# {posargs} lets callers select a subset, e.g. `tox -e py -- -m slow` for the
# stress tests, which the default run deselects.
commands = pytest {posargs}
